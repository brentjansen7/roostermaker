[build]
builder = "nixpacks"
buildCommand = "DATABASE_URL=postgresql://build:build@localhost:5432/build npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/setup-status"
healthcheckTimeout = 120
